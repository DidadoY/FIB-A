#include "diccDHashing.h"

//TODO
static vector<string> preprocess(vector<string>& words){
    sort(words.begin(),words.end());
    return words;
}


void diccDHashingFind(VVC& board, vector<string> words){
    vector<string> sortedWords = preprocess(words);
}
